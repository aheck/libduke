- Use curly braces for all blocks, even if they are only one line
- Always add a meaningful Doxygen comment in the respective header file when adding a user-facing public function
- Add meaningsful comments where it makes sense. Also consider using comments to
  structure the flow of longer functions (see duke_map_file_read_from_filename
  as reference).
